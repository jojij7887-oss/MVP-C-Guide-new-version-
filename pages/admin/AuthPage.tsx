import React from 'react';

// This component is no longer used as the login system has been removed.
// The app now defaults to a logged-in state.
const AuthPage: React.FC = () => {
  return null;
};

export default AuthPage;
